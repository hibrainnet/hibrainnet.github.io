language: ruby
rvm:
- 2.3.1

before_install:
- GIT_POST_URL=$(ruby -r "$PWD"/script/config.rb -e "puts @github_post_url")
<<<<<<< HEAD
- git clone $GIT_POST_URL
- cp research-notes/* _posts/
- ls -al _posts/
=======
- git remote add other $GIT_POST_URL
- git fetch other
#- git merge --no-edit other/master --allow-unrelated-histories
>>>>>>> c91b81ffac4068eff47a128f9dff3b9621de67ad

install:
- gem install rake
- bundle install
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: _site
  target_branch: master
  on:
    branch: source


after_success:
  - git clone https://github.com/hibrainnet/hibrainnet.github.io.git
  - cd hibrainnet.github.io
  - shopt -s extglob
  - rm -rf !(.git)
  - cd ..
  - cp -r _site/* hibrainnet.github.io/
  - cd hibrainnet.github.io
  - git add .
  - git commit -m "deploy from travis"
  - ls -al
  - git push https://$GITHUB_TOKEN@github.com/hibrainnet/hibrainnet.github.io.git master
  - cd ..
